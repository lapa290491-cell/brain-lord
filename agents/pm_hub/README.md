# Cluster: PM Hub

Оркестратор штаба.

Берёт цель Верховного Командующего, режет её на задачи, шлёт в кластеры, собирает отчёт, ставит статус `pending_approval`.

Без апрува человека вовне ничего не уходит.
